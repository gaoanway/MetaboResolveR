# Load the bundled reference source catalog

Read the provenance catalog describing the papers and databases used to
build the bundled reference table, including source type, role, and
update notes.

## Usage

``` r
load_reference_source_catalog()
```

## Value

A data.frame describing the provenance sources used by the package.
